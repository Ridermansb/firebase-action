name: 'GitHub Action with Firebase CLI and Java ready'
author: 'Riderman de Sousa Barbosa'
description: 'Wraps the firebase-tools CLI to enable common commands.'
branding:
  icon: 'package'
  color: 'gray-dark'
runs:
  using: 'docker'
  image: 'Dockerfile'
